## USER COMMENT 1

name: "USER NAME"
# USER COMMENT 2
on:
  pull_request:
    paths:
      - '**/*.yml'
      - '**/*.yaml'
      - '**/*.json'
      - '**/*.ts'
      - '**/*.js'
      - '**/*.tsx'
    # USER COMMENT 3
    branches:
      - develop
      - feature
      - alpha
      - beta
      - gamma
      - delta
      - epsilon
      # USER COMMENT 4

  workflow_call:
    inputs:
      # USER COMMENT 5
      fakevar1:
        description: 'The fake version to deploy'
        required: false
        type: string
      version:
        description: The version to deploy
        required: false
        type: string
      # USER COMMENT 7
      fakevar2:
        description: 'The fake version to deploy'
        required: false
        type: string
      baseRef:
        description: The base reference for comparison
        required: false
        type: string
    # USER COMMENT 8
    outputs:
      fakevar3:
        value: 'fakevalue3'
      fakevar4:
        value: 'fakevalue4'
  workflow_dispatch:
    inputs:
      version:
        description: The version to deploy
        required: false
        type: string
      baseRef:
        description: The base reference for comparison
        required: false
        type: string

jobs:
  # USER COMMENT 9

  fake-job-1:
    # USER COMMENT 10    
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # =============================================================================
  # CHANGES DETECTION
  # =============================================================================
  # USER COMMENT 11
  changes:
    items:
      - key:
          # =============================================================================
          # CHANGES DETECTION
          # =============================================================================
          runs-on
        value: ubuntu-latest
      - key: steps
        value:
          items:
            - items:
                - key: uses
                  value: ./.github/actions/detect-changes
                - key: with
                  value:
                    items:
                      - key: baseRef
                        value: "{ inputs.baseRef || epsilon "
                    range:
                      - 334
                      - 373
                      - 373
              range:
                - 263
                - 373
                - 373
          range:
            - 261
            - 373
            - 373
    range:
      - 213
      - 373
      - 373

  # USER COMMENT 12
  fake-job-2:
    # USER COMMENT 13
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # =============================================================================
  # TESTING JOBS
  # =============================================================================
  # Add your testing jobs here
  # Example:
  # test-api: 
  #   needs: branch
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Test API
  #       run: |
  #         echo "Run API tests"
  #
  # test-web:
  #   needs: branch
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Test Web
  #       run: |
  #         echo "Run Web tests"


  # USER COMMENT 14

  fake-job-3:
    # USER COMMENT 15
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # =============================================================================
  # VERSIONING
  # =============================================================================
  version:
    items:
      - key:
          # =============================================================================
          # VERSIONING
          # =============================================================================
          if
        value: github.ref_name == 'alpha'
      - key: needs
        value: changes
      - key: runs-on
        value: ubuntu-latest
      - key: steps
        value:
          items:
            - items:
                - key: uses
                  value: ./.github/actions/calculate-version
                - key: with
                  value:
                    items:
                      - key: baseRef
                        value: "{ inputs.baseRef || 'epsilon' "
                    range:
                      - 392
                      - 433
                      - 433
              range:
                - 318
                - 433
                - 433
          range:
            - 316
            - 433
            - 433
    range:
      - 206
      - 433
      - 433

  # USER COMMENT 16

  fake-job-4:
    # USER COMMENT 17
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # USER COMMENT 18

  fake-job-5:
    # USER COMMENT 19
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # USER COMMENT 20

  # =============================================================================
  # DEPLOYMENT JOBS
  # =============================================================================
  deploy-api:
    needs: branch
    runs-on: ubuntu-latest
    steps:
      - name: Deploy API
        run: |
          echo "Deploy API to production"

  fake-job-6:
    # USER COMMENT 21
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  # =============================================================================
  # TAG & CREATE PR
  # =============================================================================     
  tag:
    items:
      - key:
          # =============================================================================
          # TAG & CREATE PR
          # =============================================================================
          if
        value: github.ref_name == 'alpha'
      - key: needs
        value: version
      - key: runs-on
        value: ubuntu-latest
      - key: steps
        value:
          items:
            - items:
                - key: uses
                  value: ./.github/actions/create-tag
                - key: with
                  value:
                    items:
                      - key: version
                        value: "{ needs.version.outputs.nextVersion "
                    range:
                      - 390
                      - 437
                      - 437
              range:
                - 323
                - 437
                - 437
          range:
            - 321
            - 437
            - 437
    range:
      - 211
      - 437
      - 437

  # USER COMMENT 22

  fake-job-7:
    # USER COMMENT 23
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"

  createpr:
    items:
      - key:
          ## SHOULD BE ANY BRANCH EXCEPT the final branch
          if
        value: github.ref_name != 'epsilon'
      - key: needs
        value: tag
      - key: runs-on
        value: ubuntu-latest
      - key: steps
        value:
          items:
            - items:
                - key: uses
                  value: ./.github/actions/create-pr
                - key: with
                  value:
                    items:
                      - key: sourceBranch
                        value: "{ github.ref_name "
                    range:
                      - 241
                      - 275
                      - 275
              range:
                - 175
                - 275
                - 275
          range:
            - 173
            - 275
            - 275
    range:
      - 65
      - 275
      - 275

  branch:
    items:
      - key:
          ## SHOULD BE THE NEXT BRANCH
          needs
        value: createpr
      - key: runs-on
        value: ubuntu-latest
      - key: steps
        value:
          items:
            - items:
                - key: uses
                  value: ./.github/actions/manage-branch
                - key: with
                  value:
                    items:
                      - key: action
                        value: 'fast-forward'
                      - key: targetBranch
                        value: "{ github.ref_name == 'alpha' && 'beta' || github.ref_name == 'beta' &&
                          'gamma' || github.ref_name == 'gamma' && 'delta' ||
                          github.ref_name == 'delta' && 'epsilon' ||
                          github.ref_name == 'epsilon' && 'epsilon' || 'epsilon'
                          "
                    range:
                      - 190
                      - 464
                      - 464
              range:
                - 120
                - 464
                - 464
          range:
            - 118
            - 464
            - 464
    range:
      - 46
      - 464
      - 464

  # USER COMMENT 24

  fake-job-8:
    # USER COMMENT 25
    runs-on: ubuntu-latest
    steps:
      - name: Fake Job
        run: |
          echo "Running fake job"
